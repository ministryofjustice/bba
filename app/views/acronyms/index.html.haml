%a.btn.btn-primary.pull-right{ :href => new_acronym_path }
  %i.icon-plus.icon-white
  Add an Acronym

%p.toggles If you know an acronym, share it. If there is an acronym you don't know, add it so somebody can define it.

.row-fluid
  .span6

    %h1 Latest acronyms

    %dl
      - @defined_acronyms.each do |acronym|
        %dt
          = acronym.acronym
        %dd
          = acronym.definition
          = link_to acronym.url, acronym.url
          - if acronym.info.present?
            %p
              %small
                %em= acronym.info

  .span6
    %h1 Undefined acronyms

    %ol
      - @undefined_acronyms.each do |acronym|
        %li= link_to acronym.acronym, edit_acronym_path(acronym)

%table.table.table-hover.table-bordered
  %caption
    All your acronyms are belong to us! 
    %a(href="http://bit.ly/12wzRHl") What is this?
  %tr
    %th Acronym
    %th Definition
    %th URL
    / %th Info
    %th

  - @acronyms.each do |acronym|
    %tr
      %td= acronym.acronym
      %td
        - if acronym.definition.present?
          = acronym.definition
          - if acronym.info.present?
            %p
              %small
                %em= acronym.info
        - else
          = link_to "Define this Acronym", edit_acronym_path(acronym)
      %td= link_to acronym.url, acronym.url
      / %td= acronym.info
      %td
        =# link_to 'Show', acronym, :class => 'btn btn-mini'
        = link_to 'Edit', edit_acronym_path(acronym), :class => 'btn btn-mini btn-success'
        = link_to 'Destroy', acronym, :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => 'btn btn-mini btn-danger'

%br
