%a.btn.btn-primary.pull-right{ :href => new_acronym_path }
  %i.icon-plus.icon-white
  Add acronym

%p.toggles If you know an acronym, please define it here. If there is an acronym you need to know, please add it so somebody can define it.

%table.table.table-hover.table-bordered
  %caption
    All your acronyms are belong to us! 
    %a(href="http://bit.ly/12wzRHl") What is this?
  %tr
    %th Acronym
    %th Definition
    %th URL

  - @acronyms.each do |acronym|
    %tr
      %td
        - if acronym.definition.present?
          = link_to acronym.acronym, acronym_path(acronym)
        - else
          #{acronym.acronym}
      %td
        - if acronym.definition.present?
          = acronym.definition
          - if acronym.info.present?
            %p
              %small
                %em= acronym.info
        - else
          = link_to "Define #{acronym.acronym}", edit_acronym_path(acronym)
      %td= link_to acronym.url, acronym.url

%br
